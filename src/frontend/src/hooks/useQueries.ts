// This file is intentionally minimal as the backend has no data storage
// All content is static and rendered directly in components

export function useQueries() {
  // No backend queries needed for this static website
  return {};
}
